// 目标类型枚举
export enum TargetType {
  Post = 'post',
  Page = 'page',
  Article = 'article',
  Comment = 'comment',
  Video = 'video',
  Image = 'image',
  Other = 'other',
}

export enum StorageType {
  Local = 'local',
  Webdav = 'webdav',
  S3 = 's3',
}
